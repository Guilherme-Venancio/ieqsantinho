import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.ieqsantinho',
  appName: 'ieqsantinho',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
